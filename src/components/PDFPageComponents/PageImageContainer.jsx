import React from 'react'

function PageImageContainer({children}) {
  return (
    <div className="px-4 pt-4 pb-1 w-full flex flex-1 items-center justify-center">
      {children}
    </div>
  )
}

export default PageImageContainer
